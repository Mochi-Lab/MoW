// import * as actions from './actions';

const initialState = {
  web3: null,
  walletAddress: null,
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default rootReducer;
